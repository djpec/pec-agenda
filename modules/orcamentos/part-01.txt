<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Proposta DJ pec — Valores claros</title>
<style>
:root{
  --app:#ececea;
  --panel:#141414;
  --panel2:#1c1c1c;
  --paper:#fcfbf8;
  --ink:#171717;
  --muted:#6d6a66;
  --line:#ded9d2;
  --accent:#9b7658;
  --accent2:#2f5148;
  --soft:#f3eee8;
  --hero:#1d332d;
  --heroText:#fffdf9;
  --serif: Georgia, "Times New Roman", serif;
  --sans: Arial, Helvetica, sans-serif;
  --section-gap:28px;
  --hero-min:225px;
  --radius-lg:22px;
  --radius-md:16px;
  --radius-sm:12px;
  --extra-image-width:46%;
}

/* Temas por tipo de evento */
body[data-theme="casamento"]{
  --paper:#fcfaf6;
  --ink:#191714;
  --muted:#746d66;
  --line:#dfd6cc;
  --soft:#f3ede6;
  --heroText:#fffaf4;
}
body[data-theme="xv"]{
  --paper:#fcfbfd;
  --ink:#1b1720;
  --muted:#756c7d;
  --line:#dfd8e5;
  --soft:#f4eff7;
  --heroText:#fff;
}
body[data-theme="geral"]{
  --paper:#fbfcfd;
  --ink:#171b20;
  --muted:#69737d;
  --line:#dce2e7;
  --soft:#f1f4f6;
  --heroText:#fff;
}

/* Paletas */
body[data-palette="terracota"]{
  --accent:#9d7657;
  --accent2:#36564d;
  --hero:#223a33;
}
body[data-palette="azul"]{
  --accent:#496c90;
  --accent2:#2c4157;
  --hero:#213240;
}
body[data-palette="verde"]{
  --accent:#6e7d5e;
  --accent2:#324539;
  --hero:#2a3b31;
}
body[data-palette="rose"]{
  --accent:#b07a82;
  --accent2:#725c67;
  --hero:#4a3842;
}
body[data-palette="roxo"]{
  --accent:#8266a8;
  --accent2:#4e3c68;
  --hero:#342742;
}
body[data-palette="dourado"]{
  --accent:#a88b4d;
  --accent2:#544937;
  --hero:#2c2925;
}

*{box-sizing:border-box}
html,body{
  margin:0;
  padding:0;
  background:var(--app);
  font-family:var(--sans);
  color:var(--ink);
}
body{
  display:grid;
  grid-template-columns:390px minmax(0,1fr);
  min-height:100vh;
}

/* EDITOR */
.editor{
  position:sticky;
  top:0;
  height:100vh;
  overflow:auto;
  padding:22px;
  background:var(--panel);
  color:#fff;
  border-right:1px solid #2c2c2c;
}
.editor h1{margin:0 0 6px;font-size:23px}
.editor .lead{margin:0 0 17px;color:#bcbcbc;font-size:12px;line-height:1.5}
.actions{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:8px;
  margin-bottom:16px;
}
button,.file-btn{
  border:0;
  border-radius:10px;
  padding:10px 11px;
  font-size:12px;
  font-weight:700;
  cursor:pointer;
  text-align:center;
}
.primary{background:#f3f3f3;color:#111}
.secondary{background:#2c2c2c;color:#fff}
.danger{background:#4a2626;color:#ffdcdc}
.file-btn input{display:none}
details{
  margin-bottom:10px;
  overflow:hidden;
  background:var(--panel2);
  border:1px solid #343434;
  border-radius:12px;
}
summary{padding:13px 14px;font-size:13px;font-weight:700;cursor:pointer}
.fields{display:grid;gap:9px;padding:0 14px 14px}
.two{display:grid;grid-template-columns:1fr 1fr;gap:8px}
label{
  display:grid;
  gap:5px;
  color:#ddd;
  font-size:11px;
  font-weight:700;
}
input,textarea,select{
  width:100%;
  padding:9px 10px;
  background:#111;
  color:#fff;
  border:1px solid #444;
  border-radius:8px;
  font:inherit;
  font-size:12px;
  outline:none;
}
textarea{
  min-height:76px;
  resize:vertical;
  line-height:1.42;
}
input:focus,textarea:focus,select:focus{border-color:#8b8b8b}
.editor-list{display:grid;gap:10px}
.editor-item{
  padding:11px;
  background:#171717;
  border:1px solid #383838;
  border-radius:12px;
}
.editor-item-head{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-bottom:10px;
}
.editor-item-head strong{font-size:12px}
.remove{padding:6px 8px;background:#4b2727;color:#ffd9d9;font-size:10px;border-radius:8px}
.add{width:100%;background:#ededed;color:#111;border-radius:10px}
.check{display:flex;align-items:center;gap:8px}
.check input{width:auto}
.note{margin:0;color:#a9a9a9;font-size:10px;line-height:1.45}

/* STAGE */
.stage{padding:28px;overflow:auto}
.sheet{
  width:210mm;
  max-width:100%;
  margin:0 auto;
  background:var(--paper);
  box-shadow:0 18px 48px rgba(0,0,0,.13);
  border-radius:30px;
  overflow:hidden;
}
.doc{padding:14mm 15mm 13mm}
.topbar{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:20px;
  padding-bottom:15px;
  border-bottom:1px solid var(--line);
}
.brand{
  font-size:34px;
  font-weight:900;
  letter-spacing:-4px;
  line-height:1;
}
.brand small{
  margin-left:9px;
  color:var(--accent);
  font-size:8.5px;
  letter-spacing:1.6px;
  text-transform:uppercase;
  vertical-align:middle;
}
.docmeta{
  color:var(--muted);
  font-size:10px;
  line-height:1.55;
  text-align:right;
}

/* HERO */
.hero{
  position:relative;
  min-height:var(--hero-min);
  margin-top:20px;
  overflow:hidden;
  background:var(--hero);
  color:var(--heroText);
  border-radius:var(--radius-lg);
}
.hero-bg{
  position:absolute;
  inset:0;
  background-size:cover;
  background-position:center;
  opacity:.18;
  filter:saturate(.72) contrast(.92);
  transform:scale(1.03);
}
.hero-shade{
  position:absolute;
  inset:0;
  background:
    linear-gradient(90deg,rgba(0,0,0,.44) 0%,rgba(0,0,0,.21) 52%,rgba(0,0,0,.12) 100%),
    linear-gradient(0deg,rgba(0,0,0,.12),transparent 55%);
}
.hero-inner{
  position:relative;
  z-index:2;
  display:grid;
  grid-template-columns:1.18fr .82fr;
  gap:26px;
  min-height:var(--hero-min);
  padding:26px 28px;
}
.hero-kicker{
  margin-bottom:11px;
  color:rgba(255,255,255,.74);
  font-size:8.5px;
  font-weight:700;
  letter-spacing:1.8px;
  text-transform:uppercase;
}
.hero h1{
  margin:0;
  font-family:var(--serif);
  font-size:31px;
  line-height:1.07;
  font-weight:400;
  letter-spacing:-.55px;
  max-width:470px;
}
.hero p{
  margin:13px 0 0;
  color:rgba(255,255,255,.88);
  font-size:12.6px;
  line-height:1.66;
  max-width:470px;
}
.event-card{
  align-self:end;
  padding:15px 16px;
  background:rgba(255,255,255,.12);
  border:1px solid rgba(255,255,255,.19);
  backdrop-filter:blur(5px);
  border-radius:var(--radius-md);
}
.event-card .label{
  display:block;
  margin-bottom:3px;
  color:rgba(255,255,255,.65);
  font-size:7.8px;
  letter-spacing:1.15px;
  text-transform:uppercase;
}
.event-card strong{
  display:block;
  font-size:12px;
  line-height:1.5;
}
.event-card .row{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:12px;
  margin-top:10px;
}

/* FLOW */
.section{margin-top:var(--section-gap)}
.section-heading{
  display:grid;
  grid-template-columns:1fr auto;
  gap:20px;
  align-items:end;
  margin-bottom:13px;
}
.section-eyebrow{
  display:block;
  margin-bottom:5px;
  color:var(--accent);
  font-size:8px;
  font-weight:700;
  letter-spacing:1.55px;
  text-transform:uppercase;
}
.section h2{
  margin:0;
  font-family:var(--serif);
  font-size:24px;
  font-weight:400;
  letter-spacing:-.35px;
}
.section-intro{
  max-width:260px;
  color:var(--muted);
  font-size:10.8px;
  line-height:1.55;
  text-align:right;
}

/* SCOPE */
.scope-grid{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:12px;
}
.scope-grid.single{grid-template-columns:1fr}
.scope-item{
  padding:15px 16px;
  background:#fff;
  border:1px solid var(--line);
  break-inside:avoid;
  page-break-inside:avoid;
  border-radius:var(--radius-md);
}
.scope-num{
  display:block;
  margin-bottom:9px;
  color:var(--accent);
  font-family:var(--serif);
  font-size:20px;
  line-height:1;
}
.scope-item h3{
  margin:0 0 7px;
  font-family:var(--serif);
  font-size:18px;
  font-weight:400;
}
.scope-item p{
  margin:0;
  color:#444;
  font-size:11.8px;
  line-height:1.6;
  white-space:pre-line;
}
.scope-meta{
  display:flex;
  flex-wrap:wrap;
  gap:5px 11px;
  margin-top:10px;
  padding-top:9px;
  border-top:1px solid var(--line);
  color:var(--muted);
  font-size:9.8px;
}

/* INVESTMENT */
.investment{
  display:grid;
  grid-template-columns:1fr auto;
  gap:20px;
  align-items:center;
  padding:18px 19px;
  background:var(--soft);
  border-left:4px solid var(--accent);
  border-radius:var(--radius-md);
}
.investment .cap{
  display:block;
  margin-bottom:4px;
  color:var(--muted);
  font-size:8px;
  font-weight:700;
  letter-spacing:1.2px;
  text-transform:uppercase;
}
.investment .desc{font-size:11.2px;line-height:1.55;color:#454545}
.investment .price{
  font-family:var(--serif);
  font-size:27px;
  white-space:nowrap;
  color:var(--ink);
}

/* EXTRAS */
.extras-grid{
  display:grid;
  grid-template-columns:1fr;
  gap:12px;
}
.extra{
  display:grid;
  grid-template-columns:minmax(240px,var(--extra-image-width)) minmax(0,1fr);
  background:#fff;
  border:1px solid var(--line);
  break-inside:avoid;
  page-break-inside:avoid;
  border-radius:var(--radius-lg);
  overflow:hidden;
}
.extra.no-image{grid-template-columns:1fr}
.extra-media{
  position:relative;
  overflow:hidden;
  background:var(--soft);
  aspect-ratio: var(--media-ratio, 4 / 3);
  min-height:100%;
}
.extra-media img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
}
.extra-media.fit-contain img{
  object-fit:contain;
  background:var(--soft);
}
.extra-media.position-top img{object-position:center top}
.extra-media.position-center img{object-position:center center}
.extra-media.position-bottom img{object-position:center bottom}

.extra-copy{
  padding:16px 17px;
  display:flex;
  flex-direction:column;
  justify-content:flex-start;
}
.extra-kicker{
  margin-bottom:5px;
  color:var(--accent);
  font-size:7.9px;
  font-weight:700;
  letter-spacing:1.2px;
  text-transform:uppercase;
}
.extra h3{
  margin:0 0 7px;
  font-family:var(--serif);
  font-size:18px;
  font-weight:400;
}
.extra p{
  margin:0;
  color:#464646;
  font-size:11.2px;
  line-height:1.58;
  white-space:pre-line;
}
.extra-value{
  margin-top:11px;
  padding:11px 0 0 0;
  border-top:1px solid var(--line);
  font-size:11.7px;
  line-height:1.56;
  white-space:pre-line;
}
.extra-value strong{
  color:var(--accent);
  font-size:13.8px;
}

/* TERMS */
.info-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:12px;
}
.info-box{
  padding:14px 15px;
  background:#fff;
  border:1px solid var(--line);
  break-inside:avoid;
  page-break-inside:avoid;
  border-radius:var(--radius-md);
}
.info-box h3{
  margin:0 0 8px;
  color:var(--accent);
  font-size:8px;
  letter-spacing:1.3px;
  text-transform:uppercase;
}
.info-box p,.info-box li{
  font-size:10.8px;
  line-height:1.55;
  color:#444;
}
.info-box p{margin:0}
.info-box ul{margin:0;padding-left:17px}
.notes{
  margin-top:12px;
  padding:13px 14px;
  background:var(--soft);
  color:#484848;
  font-size:10.6px;
  line-height:1.58;
  border-left:3px solid var(--accent2);
  border-radius:var(--radius-md);
}

/* FOOTER */
.footer{
  display:grid;
  grid-template-columns:1fr auto;
  gap:20px;
  align-items:end;
  margin-top:26px;
  padding-top:14px;
  border-top:1px solid var(--line);
  color:var(--muted);
  font-size:9.7px;
  line-height:1.5;
}
.footer strong{color:var(--ink);font-size:10.5px}
.footer-right{text-align:right}
.contact-link{
  color:inherit;
  text-decoration:none;
  border-bottom:1px dotted rgba(0,0,0,.24);
}
.contact-link:hover{color:var(--accent)}

/* AUTO DENSITY */
.doc[data-density="compact"]{
  --section-gap:21px;
  --hero-min:198px;
}
.doc[data-density="compact"] .hero{margin-top:16px}
.doc[data-density="compact"] .hero-inner{padding:21px 24px;gap:20px}
.doc[data-density="compact"] .section-heading{margin-bottom:10px}
.doc[data-density="compact"] .scope-item{padding:13px 14px}
.doc[data-density="compact"] .investment{padding:15px 17px}
.doc[data-density="compact"] .extra-copy{padding:13px 14px}
.doc[data-density="compact"] .footer{margin-top:20px}

/* Responsive */
@media screen and (max-width:900px){
  body{display:block}
  .editor{position:relative;height:auto}
  .stage{padding:10px}
  .sheet{width:100%;border-radius:20px}
  .doc{padding:24px 20px}
  .hero-inner,.scope-grid,.info-grid{grid-template-columns:1fr}
  .event-card{align-self:auto}
  .section-heading{grid-template-columns:1fr}
  .section-intro{text-align:left;max-width:none}
  .extra{grid-template-columns:1fr}
  .extra.no-image{grid-template-columns:1fr}
}

/* Print */
@page{size:A4 portrait;margin:12mm 11mm 12mm}

@media print{
  html,body{
    display:block!important;
    width:auto!important;
    min-width:0!important;
    margin:0!important;
    padding:0!important;
    background:#fff!important;
  }
  .editor{display:none!important}
  .stage{padding:0!important;overflow:visible!important}
  .sheet{
    width:auto!important;
    max-width:none!important;
    margin:0!important;
    background:transparent!important;
    box-shadow:none!important;
    border-radius:0!important;
  }
  .doc{padding:0!important;background:transparent!important}
  .topbar,.hero,.scope-item,.investment,.extra,.info-box,.notes,.footer{
    break-inside:avoid!important;
    page-break-inside:avoid!important;
  }
  .section-heading{
    break-after:avoid!important;
    page-break-